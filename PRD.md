# PRD｜AI 情侶約會共決策 App MVP

- 狀態：Approved for MVP build
- 更新日期：2026-09-04
- 地區：大臺北
- 平台：手機 Web／PWA

## 1. 產品摘要

情侶分別輸入共同條件與個人需求，AI 在不暴露私密內容的前提下，理解「明亮、可愛但不要太幼稚、浪漫但不要太正式」等主觀語句，從真實場地資料中排出三套包含時間、交通、價格與 2–4 個站點的完整約會行程。雙方可對整套或單一站點表態，系統保留已接受部分，只局部重排衝突站點。

## 2. 問題

現有餐廳／活動平台主要提供大量清單；一般 AI 行程工具多理解單一提問者，也可能產生不存在或不可執行的地點。情侶真正的困難是：

- 兩人的偏好、預算、體力與地雷不同。
- 有些需求不方便直接對伴侶說。
- 主觀形容詞對每個人的強度不同。
- 找到地點後仍要安排順序、營業時間、交通與總預算。
- 一站不合適時，不應推翻整份已談好的行程。

## 3. 目標使用者與情境

### 3.1 使用者

- Partner A、Partner B。
- 主要為大學生及 20–30 歲情侶。
- 居住或活動於臺北市、新北市、捷運合理可達區域。

### 3.2 模式

1. **現在就出發**：從當下／集合位置與剩餘時間產生立即可走的行程。
2. **規劃指定日期**：輸入未來日期、開始／結束時間，納入需預約的餐廳或活動。

「指定日期」是 MVP 的使用模式，不代表未來產品規劃。

## 4. MVP 價值順序

1. 從不知道做什麼，到可以直接出發。
2. 理解這一對情侶，而不是只理解單一使用者。
3. 增加約會的新鮮感。
4. 減少搜尋、討論與安排時間。

## 5. Product Contract

### 5.1 使用者入口

```text
建立／加入約會房間
→ 雙方完成共同條件與個人輸入
→ 產生三套行程
→ 雙方表態與局部重排
→ 選定最終行程
→ 查看詳情與外部訂位／購票
→ 個別回饋
```

### 5.2 成功輸出

每套行程必須提供：

- 方案名稱與安全、不中傷任一方的簡短理由。
- 集合時間／地點。
- 2–4 個資料庫既有站點。
- 每站到達、離開與停留時間。
- 站點間交通方式與時間。
- 每站與總預估花費。
- 營業／活動時段驗證狀態。
- 合作優惠標示。
- 商家詳情與外部訂位／購票連結。

### 5.3 失敗輸出

無法產生三套有效方案時，系統必須：

- 保留所有硬限制，不偷偷放寬。
- 說明「目前沒有足夠可行方案」。
- 只要求雙方選擇可放寬的軟條件。
- 不以 LLM 虛構場地或時間補滿三套。

## 6. MVP 範圍

### 6.1 行程類型

- 餐廳、咖啡廳、甜點。
- 電影、展覽、手作、運動、娛樂。
- 散步、景點。
- 2–4 站的組合行程。

### 6.2 共同公開條件

- 現在／指定日期。
- 開始時間、最晚結束時間或 2 小時／半天／一天。
- 集合地點。
- 兩人總預算或每人預算。
- 理想預算與絕對上限。
- 可接受交通方式。
- 是否接受戶外活動。
- 是否要求可立即訂位／購票。

### 6.3 個人條件與可見性

每筆個人輸入可選：

- `shared`：另一半可見。
- `private_session`：只有 AI 可用，只限本次。
- `private_remembered`：只有 AI 可用，經本人同意才可納入偏好。

預設私密；私密原文、輸入者與可反推線索不可出現在共同 API、Realtime 或 UI。

### 6.4 硬限制

下列條件一律由程式驗證，LLM 無權覆蓋：

1. 日期、開始與最晚結束時間。
2. 絕對預算上限。
3. 場地地區與交通可達性。
4. 營業、活動開始／結束與入場時間。
5. 預約／票券狀態；未知時必須標示。
6. 可接受交通方式。
7. 單段與總移動時間上限。
8. 過敏、飲食禁忌、健康限制。
9. 無障礙與體力限制。
10. 年齡、身分或場域限制。
11. 使用者設定的 Hard No。
12. 戶外活動的必要天氣條件。

## 7. 功能需求

| ID | 功能 | 驗收摘要 |
|---|---|---|
| FR-01 | 建立／加入雙人 Session | 兩支手機以邀請碼進入同房，顯示在線狀態 |
| FR-02 | 公開條件同步 | 任一方修改後，另一方 2 秒內可見 |
| FR-03 | 個人私密輸入 | 對方無法從 UI、API、Realtime 取得原文 |
| FR-04 | 結構化語意解析 | 自然語言轉成固定 Schema 的硬限制、軟偏好、形容詞區間 |
| FR-05 | 形容詞敏感度 | 支援最低／最高強度、重要性、信心、Session／長期範圍 |
| FR-06 | Hybrid retrieval | SQL 硬篩選後，以 FTS＋向量召回候選 |
| FR-07 | 雙人適配計分 | 分開算 A、B，較低一方須達門檻 |
| FR-08 | 三套完整行程 | 每套 2–4 站且差異足夠，不是三個單點 |
| FR-09 | 行程可執行驗證 | 時間、營業、交通、預算全部通過 |
| FR-10 | 雙方表態 | 可 Like／Dislike 整套或指定站點與原因 |
| FR-11 | 局部重排 | 鎖定已接受站點，只替換衝突站點並重算後續 |
| FR-12 | 溝通中學習 | 「太暗／太可愛／剛好」可更新本次形容詞區間 |
| FR-13 | 隱私轉譯 | 共同理由不透露私密輸入者、原句或可反推內容 |
| FR-14 | 合作內容透明 | 合作方案清楚標示，贊助不進 CoupleScore |
| FR-15 | 外部行動 | 可開啟詳情與外部訂位／購票 |
| FR-16 | 約會後回饋 | 雙方個別評分；只有同意的資料更新長期偏好 |

## 8. 形容詞敏感度需求

系統至少支援：

- `bright` 明亮
- `cute` 可愛
- `childish` 幼稚感
- `quiet` 安靜
- `romantic` 浪漫
- `formal` 正式
- `interactive` 互動感
- `relaxing` 放鬆
- `crowded` 擁擠
- `walking_intensity` 步行強度

每個人、每個形容詞保存：

```json
{
  "attribute": "cute",
  "target_min": 0.45,
  "target_max": 0.72,
  "importance": 0.80,
  "confidence": 0.65,
  "scope": "session",
  "source": "conversation_feedback"
}
```

MVP 的「學習」是可解釋的區間／權重更新，不是執行中微調大型語言模型。

## 9. Data Contract

### 9.1 場地資料

每筆場地至少包含：

- `venue_id`、名稱、分類、行政區、座標。
- 價格區間、典型停留時間。
- 營業／活動時段、預約狀態與外部連結。
- 可檢索文字 `retrieval_text`。
- 768 維 embedding。
- 主觀屬性 0–1、來源、信心、更新時間。
- 是否為合作資料、優惠內容、是否為模擬優惠。

### 9.2 資料來源邊界

- MVP 以團隊人工整理的真實大臺北場地為底座。
- 合作優惠可模擬，但必須標示。
- Places／Routes 可補即時事實；API 失效時仍須以 Fixture 完成核心 Demo。
- 不抓取第三方評論或照片當作模型訓練資料。

### 9.3 狀態隔離

```text
data/fixtures     = 合成測試資料
data/demo         = 可公開 Demo 資料（建立後）
real user data    = 僅正式 DB，禁止進 repo
local scratch     = gitignored
```

## 10. 商業內容規則

- 合作場地必須先通過相同硬限制與適配門檻。
- 贊助不加入 CoupleScore。
- 可在已合格候選中額外展示，但必須標「合作方案」。
- 至少保留非合作方案。
- MVP 不做商家登入、CRM、後台、付款或成效報表。

## 11. 非功能需求

- 公開條件同步：目標 2 秒內。
- 三套行程產生：Fixture 模式目標 15 秒內；超時需顯示處理狀態或失敗。
- 私密原文不得進一般 log。
- API Key／Service Role Key 只在 Server。
- 所有模型輸出需 schema validation。
- 所有最終行程需 deterministic validation。
- 外部 API 失敗不得阻斷 Fixture Demo。

## 12. 驗收條件

1. 兩支手機能加入並同步共同條件。
2. A 無法從任何前端可取得管道讀到 B 私密內容。
3. 「明亮、可愛但不要太幼稚、不要走太多路」可解析成可計算條件。
4. 產生三套 2–4 站、多樣且完整的行程。
5. 預算、最晚時間、禁忌與交通零違反。
6. A、B 個別分數可檢查，較低一方達最低門檻。
7. 拒絕一站後可保留其他站點並局部重排。
8. 使用者說「太暗」後，下一輪明亮門檻與候選明顯提高。
9. 共同理由不暴露隱藏需求。
10. 合作方案透明，且非合作方案正常出現。
11. 關鍵模型／資料失敗時顯示真實錯誤，不假成功。

## 13. 非範圍

CRM、商家開發／自助後台、廣告後台、App 內付款／退款／發票、訂閱、情侶社群、公開貼文、即時聊天、遊戲化、驚喜約會、住宿／SPA／酒吧／過夜、長途旅遊、全臺或海外資料、大模型微調平台，以及任何 MVP 後續產品規劃。

完整 ASCII UI 與欄位細節見 `docs/MVP_SPEC.md`。
