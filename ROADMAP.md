# Sideby — MVP ROADMAP

## 交付原則

先鎖定資料、隱私與驗收契約，再逐段接上流程。每個階段都要留下可重跑的證據；文件完成不等於 App 完成。

2026-09-04 決策：自行部署生成模型＋Embedding 模型＋場地 RAG，不呼叫外部模型 API；前後端應用 API、內部推論介面與雙人同步保留。模型及工具型號待選，不將建議或文件存在當成已部署。

2026-09-04 黑客松補充決策：需求解析先訓練字元 TF-IDF＋Logistic Regression 小型分類器，對照固定關鍵字基準；SetFit 是有時間和失敗證據後的比較選項。不接 Google API，需求訓練資料與有證據的場地資料分開建置。以下安排是待執行工作，未收到需求表、未訓練或下載模型。

## 分工

- 前端成員：UIUX、頁面狀態、應用 API 串接與真實裝置操作。
- 使用者：應用後端、資料、權限、計分、行程驗證與整合測試。
- 模型／RAG 工作包：可交由另一位協作者負責自管模型、Embedding、索引與檢索；承接人待確認。
- 使用者提供需求表與期待答案；分類器／RAG 協作者整理訓練、評測與索引產物，後端負責契約驗證及規則整合。場地標籤和授權證據另設整理工作，不由需求模型自動猜測。
- 雙方：先定公開回應／錯誤／Session 狀態格式，前端可用標示為合成的回應開發。

## Phase 0 — 文件與資料契約

### 交付

- PRD、TDD、AGENTS 與 MVP_SPEC。
- preference-query、itinerary JSON Schema。
- 不含秘密的 venue 與 evaluation fixtures。
- 明確區分 shared、private_session、private_remembered。
- 四份權威與 MODEL_RAG／MVP_SPEC 對齊自管模型、場地檢索與無外部模型 API 邊界。

### 完成條件

- 文件中的範圍、API、欄位、驗收標準互相一致。
- schema 可解析，fixture 可通過基本結構檢查。
- Product/Data/Model/Runtime/No-Fake-Success/State-Boundary 契約已寫入。

## Phase 1 — 模型／RAG 與雙人骨架分開進行

1A 與 1B 不互相阻擋；1B 的後端由使用者先開工，1A 模型工作包可另行承接。

### 1A：獨立模型／RAG 可行性工作包

先按下述 H1–H4 完成小型分類器與資料試點；本節自管生成／Embedding 目標仍保留，但不得先投入大型模型微調。1A 不阻擋已有的 1B 房間後端或 Phase 2 私密儲存。

- 盤點實際執行主機的 CPU、RAM、GPU／VRAM 與部署位置。
- 選定可自行部署的生成／Embedding 模型、量化、授權、執行工具及索引工具；將版本與路徑寫入 TDD，尚未確認前不下載或預設特定模型。
- 整理一小批可追溯的場地描述與結構化事實，建立索引與固定繁中查詢案例。
- 跑通文字需求 → 本地解析 → schema 驗證 → 硬篩選 → 本地檢索 → 候選 venue_id 與來源。
- 記錄召回結果、否定條件、硬限制、延遲與記憶體；驗證無外部模型請求。本步僅候選檢索，不能宣稱完整行程已完成。

### 1B：雙人 Session 基礎

### 交付

- 匿名裝置身分。
- 建立房間、邀請碼、加入房間與 Session 狀態。
- 公開共同條件與雙方完成狀態。
- Realtime 同步與重連處理。
- 前後端請求／回應與錯誤契約、公開狀態讀取、兩人成員上限及第三人拒絕規則。

### 完成條件

- 兩個獨立瀏覽器能加入同一房間。
- 公開欄位能同步，私密欄位尚未接入共用事件。
- 1A 自行驗收本地模型與 RAG、來源可查、無外部模型請求及失敗狀態；不作為 1B 房間施工的前置條件。
- 1A、1B 分別保存證據，不能用其中一項通過代替另一項；前端 UIUX 可依合成契約同步進行。

### 1B 目前證據（2026-09-04）

已實作匿名憑證、邀請、兩人成員上限、公開條件、版本競爭、雙方確認、公開 GET／SSE 及在線期限。正式建置＋獨立 PostgreSQL 的 HTTP 測試已覆蓋並行加入／編輯、非成員拒絕、確認重設、雙訂閱同步、重連、應用重啟及資料庫故障。

尚待前端實際串接與兩個獨立瀏覽器／兩支手機驗收，因此不將整個 Phase 1 或 AC-01／AC-02 標為通過。後端啟動與欄位見 docs/BACKEND_API.md。

## Phase 2 — 私密輸入與結構化解析

### 交付

- 私密文字、標籤與 visibility。
- 版本化條款接受，以及可持續有效／可撤回的 `personalization_enabled`、`model_improvement_opt_in`；有效期間不逐則回饋重問。
- 自管 Natural Language Parser 固定輸出，接上 Phase 1 場地 RAG；不得轉用外部模型 API。
- 欄位隔離、日誌過濾與 Privacy Guard 初版。

### 完成條件

- A 無法讀取 B 的私密資料。
- 示例「明亮、可愛但不要太幼稚、不要走太多路」能轉成可計算查詢。
- 非法或不完整模型輸出會 fail closed。
- 私密原文不進共用向量索引，查詢快取、日誌與公開理由不可洩漏；檢索文件不能注入指令。

## Phase 3 — 推薦與三套行程

### 交付

- curated venue dataset 與交通矩陣。
- 使用已驗證場地 RAG 召回候選，資料更新後同步重建索引並核對版本。
- 硬限制過濾。
- 個人形容詞敏感度與雙人公平計分。
- 三套多站式行程、時間、成本、公開理由與合作標示。

### 完成條件

- 不違反硬限制。
- 三套方案具實質差異。
- LLM 不可創造資料；生成後仍通過程式驗證。

## Phase 4 — 局部重排與回饋學習

### 交付

- 整套／單站 like、dislike、replace。
- locked stop 與 Local Replanner。
- 本次回饋即時更新。
- 個人化設定有效時更新長期偏好。
- 私人場地清單：補充文字、自訂分類、想去／去過及喜好度 1～5 分。
- 每則回饋預設私人；作者可公開、取消公開或刪除。
- 公開短文字評論的待審／核准列表、檢舉與隱藏；不含圖片、回覆串、按讚、追蹤或私訊。
- 經模型改進同意、去識別及人工核准的下一版離線訓練候選出口。

### 完成條件

- 拒絕一站後已接受站點不被任意重做。
- 「太暗」等回饋讓下一輪相關屬性達到固定可測門檻。
- 有效個人化設定期間不逐則詢問且能更新本人偏好；設定撤回後停止新增長期更新。
- 私人清單只有本人能讀；新回饋預設私人，未核准或取消公開的內容不出現在公開列表。
- 公開可見性與模型改進同意分開；公開評論不會直接改寫場地事實、共用標籤或 RAG。
- 訓練候選可追溯至回饋、同意及資料版本，仍須人工核准與離線評測後才能形成新模型版本。

### Phase 4 建置成本與切分（規劃估算）

| 子階段 | 工作 | 相對成本 | 單人開發估算 |
|---|---|---:|---:|
| 4A | 私人清單、補充／分類、1～5 分、個人偏好更新 | 低 | 1～2 人日 |
| 4B | 條款版本、個人化／模型改進設定、撤回 | 低至中 | 1～2 人日 |
| 4C | 公開評論、取消公開、刪除、分頁 | 中 | 2～4 人日 |
| 4D | 審核、檢舉、隱藏與基本濫用防護 | 中 | 2～4 人日 |
| 4E | 同意過濾、去識別、人工核准與訓練資料版本 | 中 | 2～4 人日 |

估算以前後端既有骨架為前提，不含正式法務文案、客服及大量人工審核。黑客松若時間不足，先完成 4A、4B 與 4C 的短文字最小流程；未完成 4D 時不得開放陌生使用者自由公開投稿，未完成 4E 時不得宣稱評論已改善共用模型。

## Phase 5 — 真實雙裝置展示與收口

### 交付

- 手機直向流程。
- 生成中狀態、錯誤與降級 UI。
- 商家詳情與外部訂位／購票跳轉。
- 對抗性隱私測試、效能量測與展示紀錄。

### 完成條件

- 兩支真實裝置完成主流程。
- 外部 API 失效時核心 Demo 仍可完成。
- 自管生成、Embedding 與索引在目標部署環境通過；模型關閉時顯示失敗，不切換雲端。
- 文件、程式、測試、Runtime 與 Owner 驗收狀態分開回報。

## 明確不排程項目

CRM、商家後台、App 內付款、訂閱、完整社群（追蹤、好友、回覆串、按讚、私訊）、遊戲化、驚喜約會、住宿／SPA／酒吧／過夜、全臺資料、大型生成模型微調與大規模 RAG。受控的場地文字評論、私人清單與離線學習候選屬本次已納入範圍。

此處「大規模 RAG」指超出核准場地資料、納入大量未授權評論／照片或私人對話的系統；本次已納入的場地 RAG 屬 MVP。

## 下一刀

使用者提供有原句／標籤／群組／來源的需求表，先凍結 4–6 個屬性與標註定義，開始 H1；同時確認小區域場地試點是否有足夠證據。前端依 docs/BACKEND_API.md 串接房間，應用後端接 Phase 2 私密儲存、版本化條款與兩個持續同意設定、visibility、隔離及解析 adapter；其後依 Phase 4A～4E 完成私人清單、公開評論與學習候選。模型或索引未就緒回真實不可用狀態，不能用假結果標示完成。

## 黑客松六小時工作安排（規劃假設，非工期承諾）

假設需求表、執行電腦及最小場地材料能取得；比賽實際時長、資料準備程度與協作者仍待確認。若只有一位後端人員，以下串行縮減工作，不把可平行分工當成已有人力。

| 階段／時間預算 | 工作 | 必須產生的證據 |
|---|---|---|
| H1／第 1–2 小時 | 清理約 100–200 句需求、人工核准答案；以原句群組分 60%／20%／20% | 標籤定義、樣本來源／審核、固定切分與每類支持數 |
| H2／第 3 小時 | 固定關鍵字規則基準；訓練 CPU TF-IDF＋Logistic Regression | 訓練程式、參數、資料雜湊、模型產物與訓練耗時 |
| H3／第 4 小時 | 只看驗證組錯誤，修否定／單位規則；必要時比較 SetFit | 逐標籤指標、失敗案例、拒判／追問、採用理由 |
| H4／第 5 小時 | 接獨立場地資料及本地檢索，後端驗證硬限制 | 真實 venue_id、來源／情境、索引版本、候選與限制檢查 |
| H5／第 6 小時 | 凍結版本、最終保留題評測，完成展示與停止調參 | 固定基準比較、實際啟動、未支援範圍及 Demo 紀錄 |

訓練資料準備通常也是工作的一部分，不把六小時全算成 GPU 訓練。取得可用場地資料的時間另行計算；若材料不足，不承諾 H4 能在一小時完成。

### 場地試點工作包（建議，區域待選）

目前進度：已完成場地 JSON Schema、正規化政府資料匯入器、來源／授權／情境／人工審核政策守門與合成行為測試。已在 `.local/` 唯讀抽查新北／臺北官方 CSV，但未把真實資料匯入正式資料集，也未建立 Embedding 索引；新北可作候選骨架，臺北該檔只是行政區／主題彙整。

1. 選大臺北一個密集小區域，先整理約 12–20 個真實場地；政府資料只作候選入口，逐筆核對現況與可使用內容。
2. 事實與主觀屬性分表／分欄，記錄來源、授權、適用時段、觀察日期、審核與未知；沒有授權照片用自製圖示／文字。
3. 至少先試一組日期／預算／交通條件，確認能否產出既有規格的三套 2–4 站且有差異的方案。數量多不代表行程一定成立。
4. 不足時擴充有證據的候選或縮小已明確揭示的展示情境；硬限制不能偷偷放寬。只有合成資料時明確標示結構展示。

### 停止與交付規則

- H1 答案仍有爭議，先縮標籤範圍或追問，不直接開訓。
- 分類器若在驗證組沒有可說明收益，保留規則／使用者明確選項，誠實報告訓練嘗試與實際使用模式。
- SetFit 或本地生成模型環境拖延，不延伸大型模型調參；保存現有成果。索引未完成不宣稱 RAG 通過。
- 分類器交付包含標籤、切分、設定、產物、評測與重跑說明；RAG 交付另外包含資料／來源、Embedding／索引版本及查詢結果；兩包不能互相代替。
- 最終測試看過後不得繼續調整並仍稱它為未見測試。展示包含至少一個未見改寫、否定需求與不確定時的追問，不只背誦訓練題。
- 目前不執行分類器訓練或下載模型；政府資料匯入只以合成範例跑通，真實場地與 RAG 索引仍未完成。Phase 1B 已有的房間驗證結果不變，完整 MVP 仍未驗收。
