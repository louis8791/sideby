# Server-only PostgreSQL URL. Never prefix this with NEXT_PUBLIC_.
DATABASE_URL=postgresql://sideby:replace-me@127.0.0.1:5432/sideby
# Local development needs no credentials: use npm run dev:local instead.
